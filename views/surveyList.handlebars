<table class="table">
  <thead>
    <tr>
      <th>Survey Name</th>
      <th>Start Date</th>
      <th>End Date</th>
      <th>Status</th>
      <th>Action</th>
      <th>Survey Stats</th>
    </tr>
  </thead>
    <tbody>
      {{#each surveyCollection}}
        <tr>
          <td>{{this.surveyName}}</td>
          <td>{{this.startDate}}</td>
          <td>{{this.endDate}}</td>
          <td>{{this.status}}</td>
          <td>
            <form action="/surveylist/delete/{{this._id}}" method="post">
              <button type="submit" class="button">
                Delete
              </button>
            </form>
            <a href="/surveylist/edit/{{this._id}}">Edit</a>
          </td>
          <td>
            <a
              target="_blank"
              href="/dashboard/surveystats/{{this._id}}/{{../session.user._id}}"
            >
              Analysis
            </a>
          </td>
        </tr>
      {{/each}}
    </tbody>
</table>
